namespace MyEnums
{
    //立刻结束
    public enum CdTypes
    {
        AtOnce,
        WaitLast,
        Exhaust
    }

    //armchild死亡控制者
    public enum ControlBy
    {
        Self,
        Arm
    }

    public enum SkillPanelMode{
        Select,
        Reward
    }

    public enum AttackRange{
        tel,
        near,
    }
}